# сборка
.gradle/
.kotlin/
build/
app/build/

# путь к SDK, у каждой машины свой
local.properties

# IDE, python и локальные настройки Claude Code
.idea/
__pycache__/
.claude/

# разное
# исходные выгрузки с листом 学生名单 (ФИО, номера студенческих) - не коммитим;
# в ассеты и fixtures кладём копии, где остался только лист 课表
*.xlsx
!app/src/main/assets/*.xlsx
!fixtures/*.xlsx
# ключ подписи - в секретах репозитория, в гит не кладём
release.jks
*.base64.txt
keystore-secrets.txt

# собранные APK
*.apk

# Граф кода graphify - локальный артефакт, пересобирается командой
graphify-out/
